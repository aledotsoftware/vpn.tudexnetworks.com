name: Tudex Mesh CI/CD

"on":
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck haproxy yamllint mariadb-client jq

      - name: Setup HAProxy Mock Environment
        run: |
          mkdir -p /tmp/etc/headscale/errors
          cp -r config/errors/* /tmp/etc/headscale/errors/ 2>/dev/null || true
          touch /tmp/etc/headscale/dashboard.html /tmp/etc/headscale/admin-panel.html /tmp/etc/headscale/domain-map.txt /tmp/etc/headscale/schema.sql

      - name: Run infrastructure validation tests
        run: |
          chmod +x scripts/test-infra.sh
          ./scripts/test-infra.sh
